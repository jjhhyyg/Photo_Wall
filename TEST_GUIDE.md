# 照片墙功能测试指南

本文档提供完整的测试步骤，确保照片墙的所有功能正常工作。

## 📋 测试前准备

### 1. 配置文件检查

#### 检查 config.public.json

```bash
cat config.public.json
```

确保包含正确的函数计算 API 地址：

- `get_images_url` - 获取图片列表 API
- `upload_image_url` - 上传图片 API
- `update_metadata_url` - 更新元数据 API

#### 检查 config.private.json

```bash
cat config.private.json
```

确保包含：

- `access_key_id` - 阿里云 AccessKey ID
- `access_key_secret` - 阿里云 AccessKey Secret
- `bucket_name` - OSS Bucket 名称
- `endpoint` - OSS 接入点
- 函数计算 API 地址（与 config.public.json 相同）

---

## 🧪 测试步骤

### 第一阶段：函数计算 API 测试

#### 测试 1.1：获取图片列表 API

**目的**：验证函数计算 API 能否正确获取 OSS 中的图片列表

```bash
# 替换为你的实际 API 地址
curl "https://YOUR_GET_IMAGES_URL?limit=10&sort=date_desc"
```

**预期结果**：

```json
{
  "success": true,
  "data": [
    {
      "filename": "images/xxx.jpg",
      "url": "https://...",
      "thumbnail_url": "https://...",
      "metadata": {
        "date": "2024-01-15",
        "time": "14:30:25",
        "location": "",
        "camera": "",
        "note": ""
      }
    }
  ],
  "total": 10
}
```

**可能的问题**：

- ❌ 返回 403：检查函数计算的环境变量配置
- ❌ 返回 404：检查 API 地址是否正确
- ❌ 返回空列表：检查 OSS 中是否有图片

---

#### 测试 1.2：上传图片 API

**目的**：验证通过 API 上传图片到 OSS

创建测试脚本 `test_upload.sh`：

```bash
#!/bin/bash

# 读取测试图片并转换为 base64
IMAGE_PATH="images/0.jpg"  # 替换为实际图片路径
IMAGE_BASE64=$(base64 -i "$IMAGE_PATH")

# 调用上传 API
curl -X POST "https://YOUR_UPLOAD_IMAGE_URL" \
  -H "Content-Type: application/json" \
  -d '{
    "filename": "test_upload.jpg",
    "data": "data:image/jpeg;base64,'"$IMAGE_BASE64"'",
    "metadata": {
      "note": "API 上传测试"
    }
  }'
```

**预期结果**：

```json
{
  "success": true,
  "filename": "test_upload.jpg",
  "url": "https://..."
}
```

---

#### 测试 1.3：更新元数据 API

**目的**：验证能否更新图片元数据

```bash
curl -X POST "https://YOUR_UPDATE_METADATA_URL" \
  -H "Content-Type: application/json" \
  -d '{
    "filename": "images/test_upload.jpg",
    "metadata": {
      "date": "2024-01-20",
      "time": "15:00:00",
      "location": "30.123456,120.654321",
      "camera": "测试相机",
      "note": "元数据更新测试"
    }
  }'
```

**预期结果**：

```json
{
  "success": true,
  "message": "元数据更新成功"
}
```

---

### 第二阶段：Python 上传工具测试

#### 测试 2.1：检查依赖

```bash
# 检查 Python 版本
python3 --version

# 检查依赖是否安装
pip list | grep -E "oss2|tqdm|Pillow"
```

如果缺少依赖：

```bash
pip install oss2 tqdm pillow
```

---

#### 测试 2.2：上传工具连接测试

```bash
python3 upload_to_oss.py
```

**预期输出**：

```
============================================================
🚀 阿里云 OSS 图片上传工具（支持元数据）
============================================================

📝 使用配置文件: config.private.json
✅ 成功连接到 OSS Bucket: YOUR_BUCKET_NAME

请选择上传模式:
1. 批量上传（自动提取 EXIF）
2. 交互式上传（可为每张图片添加备注）
3. 查看已上传的图片
```

**测试各个选项**：

**选项 1 - 批量上传**：

- 选择 `1`
- 观察进度条
- 确认上传成功的文件数量

**选项 2 - 交互式上传**：

- 选择 `2`
- 为每张图片输入备注
- 确认上传成功

**选项 3 - 查看已上传图片**：

- 选择 `3`
- 查看前 20 张图片的元数据
- 确认日期、位置、备注等信息正确

---

### 第三阶段：前端照片墙测试

#### 测试 3.1：本地启动前端

```bash
# 使用 Python 启动本地服务器
python3 -m http.server 8000
```

在浏览器中访问 `http://localhost:8000`

---

#### 测试 3.2：页面加载测试

**检查项**：

- ✅ 页面标题显示正常
- ✅ 背景动画（爱心）正常显示
- ✅ 音乐播放器显示
- ✅ 纪念日天数计算正确
- ✅ 重要日期显示正确

**浏览器控制台检查**：

```javascript
// 按 F12 打开开发者工具，查看 Console
// 应该看到：
// "配置加载成功"
// "成功加载 XX 张图片"
```

---

#### 测试 3.3：图片加载测试

**检查项**：

- ✅ 初始加载 50 张图片（5批 × 10张）
- ✅ 图片缩略图正常显示
- ✅ 图片按日期排序（降序）
- ✅ 滚动到页面底部自动加载更多图片

**测试步骤**：

1. 打开页面，等待图片加载
2. 向下滚动，观察是否自动加载更多
3. 继续滚动直到所有图片加载完成

---

#### 测试 3.4：图片弹窗测试

**检查项**：

- ✅ 点击缩略图显示大图弹窗
- ✅ 弹窗显示拍摄日期
- ✅ 左右箭头切换图片
- ✅ 键盘方向键切换图片
- ✅ ESC 键关闭弹窗
- ✅ 点击关闭按钮关闭弹窗

**测试步骤**：

1. 点击任意图片
2. 测试左右箭头按钮
3. 测试键盘方向键（←、→）
4. 测试 ESC 键关闭
5. 测试点击 × 按钮关闭

---

### 第四阶段：管理后台测试

#### 测试 4.1：启动管理后台

```bash
# 使用 Python 启动本地服务器
cd admin
python3 -m http.server 8001
```

在浏览器中访问 `http://localhost:8001`

---

#### 测试 4.2：图片列表加载

**检查项**：

- ✅ 页面标题和副标题显示
- ✅ 图片网格正常显示
- ✅ 每张图片显示：
  - 缩略图
  - 文件名
  - 拍摄日期时间
  - GPS 位置（如果有）
  - 相机型号（如果有）
  - 备注（如果有）
  - "编辑"和"删除"按钮

**浏览器控制台检查**：

```javascript
// 应该看到：
// "配置加载成功"
// 没有错误信息
```

---

#### 测试 4.3：图片上传功能

**测试方式 1 - 点击上传**：

1. 点击"选择图片"按钮
2. 选择一张或多张图片
3. 观察上传进度
4. 确认上传成功（绿色提示）
5. 3秒后上传项自动消失
6. 图片列表自动刷新

**测试方式 2 - 拖拽上传**：

1. 从文件管理器拖拽图片到上传区域
2. 观察拖拽时的视觉反馈（边框高亮）
3. 松开鼠标，开始上传
4. 确认上传成功

**检查项**：

- ✅ 上传进度条正常显示
- ✅ 成功提示显示
- ✅ 失败时显示错误信息
- ✅ EXIF 信息自动提取
- ✅ 图片自动出现在列表中

---

#### 测试 4.4：搜索功能

**测试步骤**：

1. 在搜索框输入关键词
2. 观察图片列表实时过滤
3. 清空搜索框，列表恢复全部图片

**检查项**：

- ✅ 搜索结果准确（匹配备注字段）
- ✅ 搜索响应实时
- ✅ 没有匹配时显示空状态

---

#### 测试 4.5：排序功能

**测试步骤**：

1. 选择"按日期降序"
2. 确认图片按日期从新到旧排列
3. 选择"按日期升序"
4. 确认图片按日期从旧到新排列

**检查项**：

- ✅ 排序后自动重新加载图片
- ✅ 排序结果正确

---

#### 测试 4.6：编辑元数据功能

**测试步骤**：

1. 点击任意图片的"编辑"按钮
2. 弹窗显示当前元数据
3. 修改以下字段：
   - 拍摄日期
   - 拍摄时间
   - GPS 位置（格式：纬度,经度）
   - 相机型号
   - 备注
4. 点击"保存"按钮
5. 确认成功提示
6. 弹窗自动关闭
7. 图片列表自动刷新，显示新的元数据

**检查项**：

- ✅ 弹窗正常显示
- ✅ 预览图正常显示
- ✅ 表单字段预填充当前值
- ✅ 保存成功后有提示
- ✅ ESC 键可以关闭弹窗
- ✅ 点击背景可以关闭弹窗

---

#### 测试 4.7：删除功能

**注意**：当前版本的删除功能是提示性的，需要手动在 OSS 控制台删除。

**测试步骤**：

1. 点击任意图片的"删除"按钮
2. 确认弹出提示框
3. 查看提示内容

**预期结果**：

```
删除功能需要添加对应的函数计算 API。
建议直接在 OSS 控制台删除文件。
```

---

### 第五阶段：跨浏览器测试

在以下浏览器中测试前端和管理后台：

- ✅ Chrome/Edge（Chromium 内核）
- ✅ Firefox
- ✅ Safari（macOS）
- ✅ 移动端浏览器（iOS Safari、Android Chrome）

**检查项**：

- ✅ 页面布局正常
- ✅ 图片加载正常
- ✅ 交互功能正常
- ✅ 响应式设计在移动端正常

---

## 🔧 常见问题排查

### 问题 1：配置加载失败

**症状**：

- 浏览器控制台显示"配置文件加载失败"
- 页面无法加载图片

**解决方法**：

1. 检查 `config.public.json` 是否存在
2. 检查 JSON 格式是否正确（使用 [JSONLint](https://jsonlint.com/) 验证）
3. 确认文件路径正确

```bash
# 验证 JSON 格式
python3 -m json.tool config.public.json
```

---

### 问题 2：图片无法加载

**症状**：

- 页面显示"图片加载失败"
- 浏览器控制台显示 CORS 错误

**解决方法**：

1. 检查函数计算 API 地址是否正确
2. 测试 API 是否可以访问（使用 curl）
3. 检查函数计算的环境变量配置
4. 检查 OSS Bucket 权限设置

```bash
# 测试 API
curl "https://YOUR_API_URL?limit=1"
```

---

### 问题 3：上传失败

**症状**：

- 管理后台上传显示"上传失败"
- Python 脚本报错

**解决方法**：

**Python 脚本**：

```bash
# 检查配置文件
cat config.private.json

# 检查依赖
pip list | grep -E "oss2|tqdm|Pillow"

# 测试 OSS 连接
python3 -c "
import oss2
auth = oss2.Auth('YOUR_KEY', 'YOUR_SECRET')
bucket = oss2.Bucket(auth, 'YOUR_ENDPOINT', 'YOUR_BUCKET')
print(bucket.get_bucket_info())
"
```

**管理后台**：

1. 检查浏览器控制台的错误信息
2. 确认函数计算 API 可以访问
3. 检查图片大小（建议 < 10MB）

---

### 问题 4：元数据未显示

**症状**：

- 图片上传成功，但元数据为空

**解决方法**：

1. 检查图片是否包含 EXIF 信息
2. 使用 Python 脚本手动提取 EXIF
3. 使用管理后台手动添加元数据

```python
# 测试 EXIF 提取
from PIL import Image
import json

img = Image.open('test.jpg')
exif = img._getexif()
print(json.dumps({str(k): str(v) for k, v in exif.items()}, indent=2))
```

---

### 问题 5：本地服务器无法启动

**症状**：

- `python3 -m http.server` 报错端口被占用

**解决方法**：

```bash
# 使用其他端口
python3 -m http.server 8080

# 或者查找并关闭占用端口的进程
lsof -i :8000
kill -9 <PID>
```

---

## ✅ 测试检查清单

### 配置检查

- [ ] config.public.json 存在且格式正确
- [ ] config.private.json 存在且格式正确
- [ ] API 地址配置正确
- [ ] OSS 配置正确

### API 测试

- [ ] 获取图片列表 API 正常
- [ ] 上传图片 API 正常
- [ ] 更新元数据 API 正常

### Python 工具测试

- [ ] 依赖已安装
- [ ] 能连接到 OSS
- [ ] 批量上传功能正常
- [ ] 交互式上传功能正常
- [ ] 元数据提取正常

### 前端测试

- [ ] 页面正常加载
- [ ] 配置文件加载成功
- [ ] 图片列表加载正常
- [ ] 滚动加载功能正常
- [ ] 图片弹窗功能正常
- [ ] 键盘导航正常

### 管理后台测试

- [ ] 页面正常加载
- [ ] 图片列表显示正常
- [ ] 上传功能（点击）正常
- [ ] 上传功能（拖拽）正常
- [ ] 搜索功能正常
- [ ] 排序功能正常
- [ ] 编辑元数据功能正常
- [ ] 元数据保存成功

### 跨浏览器测试

- [ ] Chrome/Edge 测试通过
- [ ] Firefox 测试通过
- [ ] Safari 测试通过
- [ ] 移动端浏览器测试通过

---

## 📝 测试报告模板

测试完成后，可以记录测试结果：

```markdown
# 照片墙测试报告

**测试日期**：2024-XX-XX
**测试人员**：XXX
**测试环境**：
- 操作系统：macOS/Windows/Linux
- 浏览器：Chrome XX / Firefox XX / Safari XX
- Python 版本：3.x

## 测试结果

### API 测试
- 获取图片列表：✅ 通过 / ❌ 失败
- 上传图片：✅ 通过 / ❌ 失败
- 更新元数据：✅ 通过 / ❌ 失败

### Python 工具测试
- 批量上传：✅ 通过 / ❌ 失败
- 交互式上传：✅ 通过 / ❌ 失败
- 查看图片：✅ 通过 / ❌ 失败

### 前端测试
- 页面加载：✅ 通过 / ❌ 失败
- 图片显示：✅ 通过 / ❌ 失败
- 滚动加载：✅ 通过 / ❌ 失败
- 图片弹窗：✅ 通过 / ❌ 失败

### 管理后台测试
- 图片列表：✅ 通过 / ❌ 失败
- 图片上传：✅ 通过 / ❌ 失败
- 搜索功能：✅ 通过 / ❌ 失败
- 编辑元数据：✅ 通过 / ❌ 失败

## 发现的问题
1. [问题描述]
2. [问题描述]

## 建议改进
1. [改进建议]
2. [改进建议]
```

---

## 🎯 下一步

测试完成后，如果一切正常：

1. **部署到 GitHub Pages**

   ```bash
   git add .
   git commit -m "完成 OSS 迁移和测试"
   git push
   ```

2. **配置自定义域名**（可选）
   - 在仓库设置中添加自定义域名
   - 配置 DNS CNAME 记录

3. **监控和维护**
   - 定期检查 OSS 用量
   - 查看函数计算调用次数
   - 备份重要照片

---

**祝测试顺利！** 🎉

如果遇到问题，请参考 [DEPLOY_OSS.md](DEPLOY_OSS.md) 或提交 Issue。
